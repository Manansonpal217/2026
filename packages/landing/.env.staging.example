# Staging landing — copy to hosting env; do not commit real secrets.

NEXT_PUBLIC_API_URL=https://api-staging.tracksync.dev
NEXTAUTH_API_URL=https://api-staging.tracksync.dev
NEXTAUTH_URL=https://staging.tracksync.dev

NEXTAUTH_SECRET=

# Optional shared cookie domain for subdomains (omit for single-host staging).
# COOKIE_DOMAIN=.tracksync.dev
