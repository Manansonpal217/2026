# Staging landing — copy to hosting env; do not commit real secrets.

NEXT_PUBLIC_API_URL=https://api-staging.example.com
NEXTAUTH_API_URL=https://api-staging.example.com
NEXTAUTH_URL=https://app-staging.example.com

NEXTAUTH_SECRET=

# Optional shared cookie domain for subdomains (omit for single-host staging).
# COOKIE_DOMAIN=.example.com
